name: flutter_domain_manager
description: "Easily manage subdomains for multiple environments / tenants in Flutter web applications."
version: 0.0.4
homepage: https://iconica.app/
repository: https://github.com/Iconica-Development/flutter_domain_manager

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

